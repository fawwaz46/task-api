# Secret used to sign JWTs. Use a long random string in production.
JWT_SECRET=change-me-to-a-long-random-string

# Where the SQLite file lives. Defaults to ./task-api.db
DB_PATH=task-api.db

# Server port
PORT=3000
